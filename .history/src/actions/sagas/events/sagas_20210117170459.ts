import { call, put } from 'redux-saga/effects';
// import EventsActions, { EventsTypes } from 'actions/redux/events';
// import {  } from 'actions/redux/events/interfaces';

export function* getProductSaga() {
	// const response: AxiosResponse<Example> = yield call(api.someApi);

	// yield put(EventsActions.someAction(response.data));
}


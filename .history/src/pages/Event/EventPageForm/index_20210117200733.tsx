import * as React from 'react';
import { Field, reduxForm, InjectedFormProps } from 'redux-form';
import { Container, Row } from 'react-bootstrap';

interface Props {
	event: Event;
}

const EventPageForm = 'erez';

export default EventPageForm;
